The NHATO site header — use it at the top of every page; it sits over the hero photograph and turns opaque once scrolled.

```jsx
<Frame5 current="home" phone="0968 677 337" scrolled={false} />
```

Figma source: `Frame 5` (277:2486). Nav labels stay in English; everything else on the page is Vietnamese. In plain HTML use the `.nh-header` markup from `ui_kits/nhato-web/chrome.js` instead — that is the canonical version.
