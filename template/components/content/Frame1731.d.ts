/** Stacked thumb card: a 180px cover image over a Manrope SemiBold 16 title.
 * Figma component `Frame 1731` (261:521) — the file leaves its components auto-named,
 * so the export keeps the Figma name. Readable alias in readme.md: "ThumbCard".
 */
export interface Frame1731Props {
  image: string;
  title: string;
  href?: string;
  className?: string;
  style?: React.CSSProperties;
}
export declare function Frame1731(props: Frame1731Props): JSX.Element;

